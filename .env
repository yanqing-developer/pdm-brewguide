# Root .env (for docker-compose / shared defaults)

# Postgres connection (host network / local)
PGHOST=localhost
PGUSER=postgres
PGPASSWORD="#Karyolink-2025"
PGDATABASE=pdm_demo
PGPORT=5432

# Backend runtime
PORT=5080

# Ingest / data limits
POI_LIMIT=1000

# Recommendation tuning (backend clamp)
REC_TOP_N=10
DEFAULT_RADIUS_KM=8
MIN_RADIUS_KM=1
MAX_RADIUS_KM=120
MIN_POOL_AFTER_FILTER=5
